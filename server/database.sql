CREATE DATABASE perntodo;

CREATE TABLE crudtable(
    -- "id" SERIAL PRIMARY KEY,
    "name" VARCHAR(255),
    "email" VARCHAR(255)  ,
    "mobile" VARCHAR(255),
    "state" VARCHAR(255),
    "city" VARCHAR(255)
)
